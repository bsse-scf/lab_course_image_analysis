# --- build output ---
_build/
.jupyterlite.doit.db

# --- environments ---
.pixi/
.venv/

# --- python / jupyter cruft ---
*.pyc
__pycache__/
.pytest_cache/
.ipynb_checkpoints/
# Jupyter AI chats: created in .chat/ (see .jupyter/labconfig/), one per student
.chat/
*.chat

# --- os cruft ---
.DS_Store

# --- solutions ---
# `solutions/` is a git SUBMODULE (see AGENTS.md). Do not add ignore rules for
# paths inside it - the parent repo cannot ignore a submodule's contents, and
# solutions/.gitignore handles them instead.

# --- data too large to track ---
# The 3.3 GB challenge dataset is distributed via polybox; see
# book/setup/download_data.md. Everything else in data/ IS tracked, as plain blobs.
data/challenge/plate01.nd2
data/challenge/plate01.zip
data/_cache/

# Ignore secrets in '.env'
.env
